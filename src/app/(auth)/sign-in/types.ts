export type LoginResponseType = {
  email: string;
  expiresIn: number;
  firstName: string;
  id: string;
  lastName: string;
  refreshToken: string;
  refreshTokenExpiration: string;
  token: string;
  isSuccess: true;
  statusCode: number;
  message: string;
};
